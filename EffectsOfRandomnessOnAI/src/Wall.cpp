#include "Wall.h"
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
////draws the walls on screen
void Wall::draw(sf::RenderWindow &window)
{
	window.draw(wall);
}